
import {httpDrop} from 'hub/helpers/http-drop';
import {module, test} from 'qunit';

module('Unit | Helper | http drop');

// Replace this with your real tests.
test('it works', assert => {
  let result = httpDrop([42]);
  assert.ok(result);
});

