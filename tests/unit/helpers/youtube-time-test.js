
import {youtubeTime} from 'hub/helpers/youtube-time';
import {module, test} from 'qunit';

module('Unit | Helper | youtube time');

// Replace this with your real tests.
test('it works', assert => {
  let result = youtubeTime([42]);
  assert.ok(result);
});

