
import {youtubeVideo} from 'hub/helpers/youtube-video';
import {module, test} from 'qunit';

module('Unit | Helper | youtube video');

// Replace this with your real tests.
test('it works', assert => {
  let result = youtubeVideo([42]);
  assert.ok(result);
});

