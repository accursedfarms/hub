import youtubeTime from 'hub/utils/youtube-time';
import {module, test} from 'qunit';

module('Unit | Utility | youtube time');

// Replace this with your real tests.
test('it works', assert => {
  let result = youtubeTime();
  assert.ok(result);
});
